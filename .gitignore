# ── macOS ─────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# ── Linux ─────────────────────────────────────────────────
*~
.nfs*

# ── Windows ───────────────────────────────────────────────
Thumbs.db
ehthumbs.db
Desktop.ini

# ── IDE ───────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── Personal Claude Code overrides ────────────────────────
# CLAUDE.md is safe to commit (no personal data in this repo)
# Use CLAUDE.local.md for personal overrides — it stays local
CLAUDE.local.md
.claude/

# ── Test artifacts ────────────────────────────────────────
# Created when testing install.sh locally
test-home/
tmp/
*.test/

# ── Release-gate tooling ──────────────────────────────────
# preflight.sh check 7 needs a Python with PyYAML. Provisioned locally, never
# committed: python3 -m venv .venv && .venv/bin/pip install pyyaml
.venv/

# ── Safety net: secrets ───────────────────────────────────
.env
.env.*
*.key
*.pem
*.p12
id_rsa
id_ed25519
credentials.json
token.json
secrets.*
